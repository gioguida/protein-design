# Temperature sweep configuration
# Runs SBS at multiple temperatures, produces per-temperature plots and
# cross-temperature summary comparisons.
#
# Usage:
#   uv run python scripts/analysis/run_temperature_sweep.py \
#     --config conf/analysis/temperature_sweep.yaml

sweep:
  temperatures: [0.5, 0.7, 0.8, 0.85, 0.9, 0.95, 1.0, 1.1, 1.2, 1.5]
  beam_size: 40
  n_steps: 5          # max 5 mutations
  snapshot_every: 1
  start_mode: "top_dms"    # wt | dms | top_dms
  n_chains: 10        # maps to beam_size for wt/dms; top-k seeds for top_dms

models:
  - name: "vanilla_650M"
    checkpoint: "facebook/esm2_t33_650M_UR50D"
    model_variant: "vanilla_650M"
  - name: "evotuned_650M"
    checkpoint: "/cluster/project/infk/krause/mdenegri/protein-design/checkpoints/oas_full_evo_650m/oas_full_evo_650m.pt"
    model_variant: "evotuned_650M"
  - name: "just_dpo_650M"
    checkpoint: "/cluster/project/infk/krause/gguidarini/protein-design/checkpoints/just_dpo_650M/step_8250.pt"
    model_variant: "just_dpo_650M"
  - name: "evo_dpo_650M"
    checkpoint: "/cluster/project/infk/krause/gguidarini/protein-design/checkpoints/evo_dpo_650M/step_8250.pt"
    model_variant: "evo_dpo_650M"
# DMS reference paths used for beam_pll_vs_dms_histogram and beam_aa_heatmap.
# Leave null to skip DMS-dependent plots.
dms:
  m22_path: "/cluster/project/infk/krause/${USER}/protein-design/data/raw/ED2_M22_binding_enrichment.csv"      # path to D2_M22.csv (or equivalent)
  si06_path: "/cluster/project/infk/krause/${USER}/protein-design/data/raw/ED2_SI06_binding_enrichment.csv"      # path to D2_SI06.csv (optional)
  m22_col: "M22_binding_enrichment_adj"
  si06_col: "SI06_binding_enrichment_adj"
  max_dms: 500

# Which plots to produce per temperature point.
# beam_pll_trajectory routes to the existing gibbs_diagnostics.py.
# DMS-dependent plots (beam_pll_vs_dms_histogram, beam_aa_heatmap) require
# dms.m22_path to be set.
plots:
  beam_pll_trajectory: true
  beam_pll_vs_dms_histogram: true
  beam_diversity_diagnostics: true
  beam_pll_vs_nmut: true
  beam_aa_heatmap: true

# Summary comparison plots across temperatures.
summary:
  fraction_above_wt: true          # % of final-step sequences with PLL > WT PLL
  pll_vs_enrichment_scatter: true  # per-temperature PLL vs DMS enrichment panels
  top_k_enrichment_recovery: true  # mean DMS enrichment of top-k PLL sequences
  pll_vs_diversity_tradeoff: true   # mean PLL of top-k vs mean pairwise Hamming
  pll_distribution_by_temp: true    # overlaid PLL histograms, one per temperature
  entropy_heatmap: true             # position-wise Shannon entropy across temperatures
  jsd_vs_temp: true                 # mean/per-position JSD (SBS vs DMS) across temperatures
  novelty_analysis: true            # stacked bar: generated CDRHs found vs. novel across all datasets
  top_k_for_pll: 50                 # top sequences used for mean PLL in tradeoff plot

output:
  base_dir: "/cluster/project/infk/krause/${USER}/protein-design/reports/beam_temperature_sweep/outputs"
  plots_dir: "/cluster/project/infk/krause/${USER}/protein-design/reports/beam_temperature_sweep/plots"
