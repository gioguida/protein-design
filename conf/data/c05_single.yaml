# @package _global_
# TTT uses a single sequence and does not consume the train/val/test split.
data:
  fasta_path: ${paths.project_dir}/datasets/c05/c05_single.fasta
  max_seq_len: 256
  mlm_probability: 0.15
