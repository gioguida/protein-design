# @package _global_
defaults:
  - override /data: evo/c05_5k
  - override /scoring: d2
  - _self_

task:
  name: evotuning_c05
  runner: evotuning
  stage_type: evotuning

training:
  learning_rate: 5.0e-5
  warmup_steps: 50
  max_epochs: 10
  max_steps: null
  batch_size: 128
  gradient_accumulation_steps: 4
  save_every_n_steps: 100
  fp16: true
